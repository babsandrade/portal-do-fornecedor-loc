import MainHeader from 'src/components/table/mainHeader'

const TableHeader = () => {
  // ** Props

  return <MainHeader title='Serviço(s)' addButtonIcon='plus' addButtonHref='/apps/invoice/add' />
}

export default TableHeader
